<?xml version="1.0" encoding="utf-8"?>
<!-- The locales the app actually ships strings for. Android 13+ reads this to offer a
     per-app language, which a preloaded first-party app is expected to support; every
     entry here must have a values-<lang> folder behind it. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android"
    android:defaultLocale="en">
    <locale android:name="en" />
    <locale android:name="ar" />
    <locale android:name="de" />
    <locale android:name="es" />
    <locale android:name="fr" />
    <locale android:name="hi" />
    <locale android:name="it" />
    <locale android:name="ja" />
    <locale android:name="ko" />
    <locale android:name="pt-BR" />
    <locale android:name="ru" />
    <locale android:name="zh-CN" />
</locale-config>
